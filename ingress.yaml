apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enhanced-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enhanced-service
            port:
              number: 80